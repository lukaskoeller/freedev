name: sveltekit-adapter-main
runtime: nodejs
description: SvelteKit adapter to deploy SvelteKit to AWS using Pulumi (main stack)
template:
  config:
    aws:profile: iac
    aws:skipCredentialsValidation: "true"
    aws:region:
      description: The AWS region to deploy into
      default: eu-central-1
